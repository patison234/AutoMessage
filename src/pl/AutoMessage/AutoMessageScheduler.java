package pl.AutoMessage;

import org.bukkit.event.Listener;

public class AutoMessageScheduler implements Listener {
	
	Main plugin;
	
	public AutoMessageScheduler(Main plugin) {
		this.plugin = plugin;
	}
}
